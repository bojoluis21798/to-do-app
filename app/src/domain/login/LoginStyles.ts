import {StyleSheet} from 'react-native';

const styles = StyleSheet.create({
  container: {
    padding: 25,
  },
  title: {
    textAlign: 'center',
    marginBottom: 20,
  },
  input: {
    width: '100%',
    marginBottom: 10,
    marginTop: 10,
  },
  loginContainer: {
    width: '100%',
    marginBottom: 10,
    marginTop: 10,
  },
});

export default styles;
